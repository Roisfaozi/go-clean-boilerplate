version: "3.8"

services:
  # Load Balancer / Gateway
  nginx:
    image: nginx:alpine
    container_name: nginx_gateway
    ports:
      - "80:80"
    volumes:
      - ./deploy/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./deploy/nginx/upstream.conf:/etc/nginx/conf.d/upstream.conf
    depends_on:
      - app-blue
      - app-green
    networks:
      - app_network

  # Blue Instance
  app-blue:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    container_name: app-blue
    environment:
      - SERVER_APP_ENV=production
      - INSTANCE_ID=blue
      - MYSQL_HOST=mysql_prod
      - MYSQL_PORT=3306
      - MYSQL_DBNAME=${MYSQL_DBNAME}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - REDIS_ADDR=redis_prod:6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - SUPERADMIN_PASSWORD=${SUPERADMIN_PASSWORD}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - METRICS_ENABLED=${METRICS_ENABLED:-true}
      - METRICS_AUTH_ENABLED=${METRICS_AUTH_ENABLED:-true}
      - METRICS_USERNAME=${METRICS_USERNAME}
      - METRICS_PASSWORD=${METRICS_PASSWORD}
      - OTEL_ENABLED=${OTEL_ENABLED:-false}
      - OTEL_SERVICE_NAME=${OTEL_SERVICE_NAME:-go-clean-api}
      - OTEL_COLLECTOR_URL=${OTEL_COLLECTOR_URL:-localhost:4317}
      - SERVER_TRUSTED_PROXIES=${SERVER_TRUSTED_PROXIES}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
    networks:
      - app_network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Green Instance
  app-green:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    container_name: app-green
    environment:
      - SERVER_APP_ENV=production
      - INSTANCE_ID=green
      - MYSQL_HOST=mysql_prod
      - MYSQL_PORT=3306
      - MYSQL_DBNAME=${MYSQL_DBNAME}
      - MYSQL_USER=${MYSQL_USER}
      - MYSQL_PASSWORD=${MYSQL_PASSWORD}
      - REDIS_ADDR=redis_prod:6379
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - JWT_ACCESS_SECRET=${JWT_ACCESS_SECRET}
      - JWT_REFRESH_SECRET=${JWT_REFRESH_SECRET}
      - SUPERADMIN_PASSWORD=${SUPERADMIN_PASSWORD}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - METRICS_ENABLED=${METRICS_ENABLED:-true}
      - METRICS_AUTH_ENABLED=${METRICS_AUTH_ENABLED:-true}
      - METRICS_USERNAME=${METRICS_USERNAME}
      - METRICS_PASSWORD=${METRICS_PASSWORD}
      - OTEL_ENABLED=${OTEL_ENABLED:-false}
      - OTEL_SERVICE_NAME=${OTEL_SERVICE_NAME:-go-clean-api}
      - OTEL_COLLECTOR_URL=${OTEL_COLLECTOR_URL:-localhost:4317}
      - SERVER_TRUSTED_PROXIES=${SERVER_TRUSTED_PROXIES}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
    networks:
      - app_network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/api/v1/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Database & Cache
  mysql_prod:
    image: mysql:8.0
    container_name: mysql_prod
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DBNAME}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - mysql_data_prod:/var/lib/mysql
    networks:
      - app_network

  redis_prod:
    image: redis:alpine
    container_name: redis_prod
    command: redis-server --requirepass ${REDIS_PASSWORD}
    networks:
      - app_network

networks:
  app_network:
    driver: bridge

volumes:
  mysql_data_prod:
